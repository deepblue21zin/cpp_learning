/*fast.cpp 규칙(고정)

큰 배열은 local 금지(필요하면 clean에서 static로)

곱/합 나오면 1LL * a * b 습관

디버그는 DLOG()로만 찍고 제출 전에 -DDEBUG 빼기*/


// fast.cpp - BOJ XXXXX
// Step0:
// CONSTS:
// TYPE:
// MEM:

#include <iostream>
using namespace std;

#ifdef DEBUG
  #define DLOG(msg) do { cerr << msg << '\n'; } while (0)
#else
  #define DLOG(msg) do {} while (0)
#endif

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // TODO: input

    // TODO: solve

    // TODO: output
    return 0;
}